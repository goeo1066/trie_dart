name: trie_dart
description: >-
  A generic Trie (prefix tree) for Dart with synonym-based path chaining
  and sequential keyword search.
version: 0.1.0
repository: https://github.com/goeo1066/trie_dart
topics:
  - trie
  - data-structure
  - search
  - prefix-tree
  - text-search

environment:
  sdk: ^3.8.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
